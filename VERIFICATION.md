# 검증 기록

검증 기준일: 2026-08-03

## 요구사항별 증거

| 요구사항 | 구현 및 검증 증거 | 상태 |
| --- | --- | --- |
| `스파이더맨: 브랜드 뉴 데이` 제목 변형 인식 | `src/matcher.js`의 NFKC 정규화와 문장부호 제거, `test/matcher.test.js` | 로컬 검증 완료 |
| 상영 형식 선택 또는 전체 감시 | 빈 `formats`는 전체 형식, 값이 있으면 포맷·상영관·등급 필드로 선택 | 로컬 검증 완료 |
| 예매 가능한 새 회차만 알림 | 예매 종료·매진·판매 통제·잔여석 0 제외 테스트 | 로컬 검증 완료 |
| 대상 지점별 5분 주기 확인 | Cloudflare `*/5 * * * *` 외부 호출기와 GitHub 실행 요약의 예약·시작 시각 비교 | 로컬 검증 완료, 실환경 배포 대기 |
| 오픈 상태 전환 감지 | 예매 준비 상태 제외 후 판매 가능 전환 시 최초 1회만 대기열 등록 | 자동 전환 테스트 완료 |
| Discord 연결 점검 | 실제 알림과 구분되는 `CGV Open Watch 테스트` 수동 전송 기능 | 실환경 검증 완료 |
| 재시작 후 중복 방지 | 지점·영화·날짜·상영관·시간 키와 Git 상태 파일, 동일 일정 2회 실제 조회 | 로컬 검증 완료 |
| 알림 실패 복구 | version 2 영속 대기열, 성공 건만 제거, 부분 실패 통합 테스트 | 로컬 검증 완료 |
| Discord 알림 폭주 및 길이 제한 방지 | 같은 영화·극장·날짜의 회차를 묶고 1,900자 이내로 자동 분할 | 로컬 검증 완료 |
| 오류 및 요청 제한 재시도 | CGV 429/5xx와 Discord 429/5xx 제한 재시도 | 로컬 검증 완료 |
| 비밀값 보호 | Webhook 환경변수 사용, `allowed_mentions` 차단, 로그 미노출 통합 테스트 | 로컬 검증 완료 |
| 장기 운영 중단 방지 | 30일 상태 갱신으로 공개 저장소 60일 미활동 중단 예방 | 로컬 검증 완료 |
| 접근 제한 우회 금지 | 로그인·CAPTCHA 없이 공식 극장 페이지를 열고 같은 출처의 읽기 전용 일정만 확인 | 로컬 검증 완료 |
| 무료 운영 | 공개 저장소의 표준 GitHub 호스팅 실행기 사용 | 배포 설정 완료 |

## 실행한 검증

```text
npm test                         28개 테스트 통과
npm run lint                    JavaScript 문법 검사 통과
npm audit --omit=dev            알려진 취약점 0개
actionlint watch.yml            GitHub Actions 문법 검사 통과
npm run check -- --dry-run      실제 CGV 공개 일정 조회 성공
wrangler deploy --dry-run       5분 Cron Worker 번들·바인딩 검증 통과
```

기본 설정인 전체 형식 감시의 실제 읽기 전용 조회에서는 전체 일정 484개 중 예매 가능한 대상 회차 465개를 선택했습니다. 일반 2D, IMAX LASER 2D, SCREENX, ULTRA 4DX 형식을 모두 포함했고, `--notify-existing` 검증에서는 465개 회차를 메시지 13건으로 나눴습니다. 가장 긴 메시지는 안전 여유를 둔 제한인 1,900자보다 작은 1,897자였습니다.

## GitHub·Discord 실환경 검증

공개 저장소 `twy8939/cgv-open-watch`와 실제 Discord Webhook으로 다음을 확인했습니다.

1. 공개 저장소의 `workflow_dispatch` 실행 성공
2. GitHub 호스팅 Ubuntu 실행기에서 Chrome을 통한 CGV 접근 성공
3. `DISCORD_WEBHOOK_URL` Secret이 로그에서 `***`로 가려지는 것을 확인
4. 미전송 회차 6개를 Discord 메시지 4건으로 실제 전송
5. 성공 후 원격 대기열이 0개로 비워지는 것을 확인
6. 즉시 재실행에서 신규 회차 0개, Discord 메시지 0건으로 중복 방지 확인
7. 새 `send_test_notification` 옵션으로 실제 Discord 테스트 메시지 전송 성공
8. 같은 실행에서 전체 일정 377개, 예매 가능 357개, 신규 0개, 추가 알림 0건 확인

실제 전송 실행: `30777175076`  
중복 방지 재실행: `30777294884`
테스트 알림 및 실행 요약: `30777901049`

예약 표현식은 5분 간격이지만 실제 `schedule` 실행은 약 1시간 간격으로 일부만 생성됐습니다. 이는 GitHub 예약 실행의 지연·누락 가능성을 실측한 결과이며, GitHub Actions만으로 5분 이내 감지를 보장할 수 없다는 운영 제한으로 남습니다.

이를 보완하기 위해 Cloudflare Cron이 5분마다 GitHub `workflow_dispatch`를 호출하는 외부 스케줄러를 추가했습니다. 외부 호출에는 해당 저장소의 Actions 쓰기 권한만 가진 Fine-grained token을 사용하고, 예약 시각을 워크플로 입력으로 전달해 실제 시작 지연을 실행 요약에 남깁니다. Cloudflare 실환경 배포 후 연속 3회 간격 측정이 남아 있습니다.

GitHub 예약 실행은 플랫폼 혼잡으로 지연될 수 있으므로 5분은 설정된 확인 주기이며 절대적인 전달 시간 보장은 아닙니다. 또한 Discord 전송 성공과 Git 완료 상태 커밋은 서로 다른 시스템이므로, 전송 직후 실행기가 강제 종료되는 극히 짧은 구간에는 한 차례 중복 가능성이 남습니다. 일반 오류와 재시작에서는 영속 대기열이 누락을 방지합니다.
